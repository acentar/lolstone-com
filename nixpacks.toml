# Coolify/Nixpacks: use npm install so install works when lock file has peer/optional mismatches.
# npm ci fails with "Missing: @react-native-async-storage/async-storage@1.24.0 from lock file".
[phases.install]
cmds = ["npm install"]
